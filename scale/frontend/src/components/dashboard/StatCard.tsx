// StatCard has been promoted to @spoolhard/ui so it's identical across
// products. Keep this file as a re-export to avoid churning import sites.
export { StatCard } from '@spoolhard/ui/components/StatCard';
